---
import fetchData from "../lib/fetchData";
import type FetchData from "../interfaces/FetchData";
import Layout from "../layouts/Layout.astro";
import Team from "../components/sections/about/Team.tsx";
import type TeamItem from "../interfaces/TeamItem";

const query = `
query Query {
	teamModel {
    name
    title
    photo
    quote
		story {
			header
			subheader
			photo
			slides {
				date
				text
			}
		}
		award
    counters {
      number
      text
    }
    certificates {
      title
      text
      image
    }
  }
}
`;

const { teamModel: data }: FetchData = await fetchData(query);
---

<Layout title="O nás" description="">
	<Team teamItems={data as TeamItem[]} client:load />
</Layout>